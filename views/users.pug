extends layouts/main
block styles
    link(rel="stylesheet", href="/css/users.css")
block content
    main
        if users.length > 0
            ul.users-list
                each user in users
                    li #{user.name}
        else
            h1 No users